"cli-trigger" is a NodeJS application designed purely for triggering twitch events to a given callback URL

Usage:

- POST to "/trigger" with query params "event=[eventName]" and "callback=[callbackURL]" to trigger twitch events

(Omit "[]")